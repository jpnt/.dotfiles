#!/bin/sh
set -eu

exec 9>"${XDG_RUNTIME_DIR:-/tmp}/grim.lock"
flock -n 9

case "$1" in
    full)   grim ;;
    region) grim -g "$(slurp)" ;;
    *)      echo "usage: $0 [ full | region ]" >&2; exit 1 ;;
esac
